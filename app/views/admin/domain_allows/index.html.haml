- content_for :page_title do
  = t('admin.domain_allows.title')

.table-wrapper
  %table.table
    %thead
      %tr
        %th= t('admin.domain_allows.domain')
        %th= t('admin.domain_allows.severity')
        %th= t('admin.domain_allows.reject_media')
        %th
    %tbody
      = render @domain_allows

= paginate @domain_allows
= link_to t('admin.domain_allows.add_new'), new_admin_domain_block_path, class: 'button'
